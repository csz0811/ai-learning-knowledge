# W6 周六测验评分

**总分：83 / 100 ✅**

---

## 🚨 闭卷部分（50分）→ 45分

| 题 | 你的答案 | 结果 | 解析 |
|:---:|:---:|:---:|---|
| Q1 | A | ✅ | `string` 是合法 type |
| Q2 | D | ❌ | 扣5分。正确：B。`messages + response_format` 是调用 LLM 的**两层结构** |
| Q3 | B | ✅ | `additionalProperties: false` = 禁止额外字段 |
| Q4 | B | ✅ | Markdown 分隔线 `---`（`***` 也行但课程学的是 `---`） |
| Q5 | A | ✅ | Few-shot 正确结构 |
| Q6 | B | ✅ | 经典 CoT 触发句 `"Let's think step by step"` |
| Q7 | 角色+任务+约束 | ✅ | 三要素全对 |
| Q8 | 只输出字段里的信息 | ⚠️ | 扣5分。`required` 意思是「输出时**必须包含**这些字段」，不是「只输出」。表述偏了 |
| Q9 | ❌ | ✅ | JSON 不允许注释，写了会报错 |
| Q10 | ❌ | ✅ | schema 是**输出格式描述**，不是输入要求 |

---

**闭卷根因：Q2 错了**
> **两层结构**：`messages`（对话内容）+ `response_format`（输出格式控制）。不是在说 API Key/Endpoint，那个叫「认证参数」不叫两层结构。

---

## 🚀 开卷部分（50分）→ 38分

### Q11（10分）→ 8分
> 你的：Markdown=给人看 / JSON=给机器看

✅ 方向对。扣2分：JSON Schema 也可以给人看（调试/文档），核心区别是「需不需要程序解析」。举例可以更具体（如：Markdown=商品详情页 / JSON=API数据交换）。

---

### Q12 角色设定 Prompt（10分）→ 9分
✅ 三要素齐全（角色+任务+约束）
⚠️ 小扣1分：约束写得稍散（两条约束可合并成一句更简洁的）
> 修正后：`只能根据用户给出的事实进行分析，输出简单易懂`

---

### Q13 Few-shot Prompt（10分）→ 6分
⚠️ 扣4分：

1. **模板占位符没填示例**：「用户输入：」「输出示例：」是空的，没给真实输入→输出对
2. **结构不完整**：少「新输入放最后」那一步
3. **没有触发句**：不知道这段 Prompt 最后给的新输入是什么

> 修正示例：
> ```
> 任务：你是情感分析师，根据评论输出情感
> 示例：
> 输入：这个东西太垃圾了，等了一周才到
> 输出：negative（负面，理由：等待时间长，表达不满）
> 输入：超喜欢！朋友都说好看
> 输出：________（这是你的新输入，在这里）
> ```

---

### Q14 CoT Prompt（10分）→ 7分
⚠️ 扣3分：

1. **触发句格式不对**：`lets thinking step by step` 小写 + 没加引号，不是标准触发句
2. **示例结构同 Q13**：空占位符，没给真实输入→输出对

> 修正：`请一步一步思考：`（中文触发句也可以）
> 引号引起来：`"请一步一步思考："`

---

### Q15 踩坑复盘（5分）→ 4分
✅ 诚实承认「输入输出混淆」——这是本周最大坑，继续重视。

---

### Q16 W7 担心（3分）→ 3分
✅ 这个担心是合理的。API 调用比 Prompt 更近代码，**防御性编程**要求会更高（request 失败处理、超时、JSON 解析错误）。但你 W1-W4 的 Python 基础在，W7 会更容易。

---

## 📊 总分汇总

| 模块 | 得分 | 满分 |
|---|:---:|:---:|
| 闭卷 | 45 | 50 |
| Q11 | 8 | 10 |
| Q12 | 9 | 10 |
| Q13 | 6 | 10 |
| Q14 | 7 | 10 |
| Q15 | 4 | 5 |
| Q16 | 3 | 3 |
| **总计** | **83** | **100** |

**等级：75–89 ✅（本周首次综合测验拿到✅）**

---

## 🔴 根因分析

### 本次 Top3 扣分点

**① Q2「两层结构」概念不清**（-5分）
> 不是 API Key + Endpoint，那是认证参数。正确：`messages`（对话）+ `response_format`（输出格式控制）

**② Few-shot 示例留空**（Q13 -4分）
> 你写了模板但没填真实输入→输出对。Few-shot 的核心价值就是「示例」，没示例等于没用模板

**③ CoT 触发句格式错误**（Q14 -3分）
> `lets thinking step by step` → 应该是 `"Let's think step by step"`（大写 L、引号包起来）
> 中文也可以：`"请一步一步思考："`

---

## ✅ 下周衔接（W7 前必做清单）

1. **区分「认证参数」vs「调用结构」**：API Key 是钥匙，messages+response_format 是内容
2. **Few-shot 每次必须填真实示例**：模板是骨架，示例是血肉
3. **CoT 触发句背下来**：`"Let's think step by step"` 或 `"请一步一步思考："`
4. **JSON Schema 区分输入/输出**：schema 是模型**输出**的形状，不是用户输入

---

## 🧠 复盘三问

**1. 本周最大收获？**
W6 终于把 Schema 字段 = 模型输出讲通了（周四🌟90分证明你真的懂了），模板库也整理完了。

**2. 哪个坑还会再踩？**
「输入输出混淆」——你 Q15 自己说了，我也认为这个坑容易反复，因为接近「思维方式」级别。下周每次写 Schema 前先问自己一句：「这是在描述用户给模型的数据，还是模型应该吐出来的数据？」

**3. W7 最大担心？**
你说「怕之前没掌握影响整体进度」。这个担心**正常且合理**——但你 W1-W4 的 Python 基础扎实，W7 只是把这些技能搬到 API 调用场景。W3 的函数封装 + W4 的异常处理（try/except）直接可以用上。**你的底子够用。**

---

**评分人：助教 AI**
**评分时间：2026-08-14 08:37**