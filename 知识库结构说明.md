# 知识库结构说明（任何 AI 工具都先读这份）

> **这是给 AI 工具看的"使用手册"。** 不管是 QClaw / Codex / Claude / 其他 agent，接手这个知识库时，**先读 `资产库/业务背景包.md` 拿到上下文，再读本文件拿到录入规则**，然后严格按规则增删改。
> 本库是**工具无关**的通用大脑，纯 markdown，任何软件可读写。
> **总导航入口**：`知识库总索引(MOC).md`（跨三层链接目录，任何工具先读这里再深入）。

## 一、四层结构（内容归哪层，别放错）
| 层 | 目录 | 放什么 | 谁用 |
|---|---|---|---|
| ① 学习层 | `00-学习路线图.md` + `0X-WeekX.md` + `00-每日练习/` | 学习过程、作业、评分、复习清单 | 未来的你复习概念 |
| ② 资产层 | `资产库/` | **可复用产物**：业务背景包、踩坑手册、代码配方卡 | **未来做项目 / 接任何工具** |
| ③ 项目层 | `项目库/<项目名>/` | 真实项目的需求/数据/部署/迭代日志 | 具体交付物 |
| ④ 方法论 | `方法论/` | **元知识**：产品思维/架构/可落地/与AI协作/学习方法论 | 做项目、带AI时的"怎么想" |

## 二、命名规范
- 周总文档：`XX-WeekX-主题.md`（如 `10-Week10-向量库.md`），根目录一份。
- 每日文件：`00-每日练习/WX-DY-主题.md`（如 `W11-D1-LangChain安装与DocumentLoader.md`），一天一份。
- 配方卡：`资产库/代码模板/<功能>_<技术>_<版本>.md`（如 `rag_pipeline_langchain1x.md`）。
- 项目空间：`项目库/<项目名>/`（如 `项目库/直播FAQ机器人/`）。
- 方法论卡片：`方法论/<主题>.md`（如 `方法论/产品思维与AI协作框架.md`）。

## 三、各类内容的录入规则
1. **新学知识点** → 写进对应 `0X-WeekX.md` 与 `00-每日练习/`；周日汇总成周总（含复习清单）。
2. **可复用代码** → 落成 `资产库/代码模板/` 的**配方卡**，格式：`场景 → 依赖 → 完整可跑代码 → 注意事项`。必须是验证能跑的，不是作业。
3. **踩过的坑** → 追加到 `资产库/踩坑手册.md`，格式：`遇 X → 做 Y`。
4. **业务/环境/进度变化** → 更新 `资产库/业务背景包.md`（这是给外部工具的第一份上下文）。
5. **真实项目** → 在 `项目库/` 开子目录，按 需求/数据/部署/日志 组织；其中可复用代码提炼回 `资产库/代码模板/`。

## 四、给 AI 工具的铁律
- ❌ 不要把大段聊天流水、原始日志写进本库根目录或资产层（那是 `memory/每日` 与 `学习档案/` 的活，本库不存）。
- ❌ 不要新增无意义的根目录；新内容必须归入上述四层之一。
- ✅ 保持 `00-学习路线图.md` 为唯一总入口（进度状态在那里更新）。
- ✅ 任何写入都先想"未来另一个工具读得懂吗"——用工具无关的语言，别写只有某个软件才懂的格式。

## 五、新增知识入库治理规则（外部来源）
用户从其他地方（短视频/文章/其他 AI/书）带来新知识时，**不静默写入**，按此流程：
1. **先讨论辨析**：就内容沟通，用 5 条标准评估（服务目标/工具无关/自文档化/低维护/旧内容可迁），不盲从。
2. **用户拍板是否添加**：讨论后由用户决定加不加，助手不擅自入库。
3. **归类由助手建议、用户确认**：能归入现有四层（学习/资产/项目/方法论）→ 存入对应分支；是全新类别 → 助手提议新建库/分支，用户确认后建。
4. **蒸馏入库**：落成可复用卡片（像配方卡），不堆原文、不记流水。

## 六、Skill 管理规则
> 知识库是 skill 的中央源文件，各工具（OpenClaw / Claude Code 等）各读各的。

**目录位置：** `资产层/skills/`

**三层管理：**

| 层级    | 位置                             | 作用                    |
| ----- | ------------------------------ | --------------------- |
| 源文件   | `资产层/skills/<skill名>/SKILL.md` | 唯一源文件，所有工具从这里同步       |
| 同步脚本  | `资产层/skills/sync-skills.sh`    | 把源文件同步到目标工具的 skill 目录 |
| 防滥用指南 | `资产层/skills/skill防滥用指南.md`     | 写 skill 描述的规则，避免乱触发   |

**新增 skill 流程：**
```
1. 发给小Q → 小Q 判断：有没有用 / 触发条件怎么写 / 同步到哪些工具
2. 小Q 写好 → 存进 `资产层/skills/<skill名>/SKILL.md`
3. 学员运行 sync-skills.sh → 同步到目标工具
4. 重启工具 → skill 生效
```

**同步目标工具（当前已支持）：**
- OpenClaw：`~/.qclaw/skills/`
- Claude Code：`~/.claude/skills/`

**触发条件写法规：**
```markdown
Use this skill when [精准触发场景]。
NOT for: [不应该触发的情况]。
```

**禁用不兼容工具（写进 SKILL.md frontmatter）：**
```markdown
---
DISABLED-FOR: openclaw
REASON: 这个 skill 依赖某工具的特殊能力，OpenClaw 无法完整执行。
USE-INSTEAD: 知识库里的对应文件路径
---
```

**新增约定（v1.2，2026-08-20）：**
- **适用方标注**：每个 skill 评估时标注 `PRIMARY-USER: 工具名`（正向标谁主用），配合已有的 `DISABLED-FOR:`（反向标谁不能用）。适用方分类：教学讲解类→QClaw；架构审查沟通类→小舟；写码编程类→Codex；通用→全部；用户自用→用户。
- **登记**：新增 skill 同步登记到 `资产层/skills/_skill登记表.md`（名称/功能一句话/适用方/来源/日期/状态）。
- **双端路径（待改造）**：`sync-skills.sh` 当前写死 Mac `$HOME` 路径，Windows 纯 PowerShell 下会失败；v1.2 要求改为「OS 检测 + KB_HOME 环境变量兜底」，Codex 进场后加它的同步目标（等效 `~/.codex/skills/`）。改造待用户提供 Windows 知识库路径后执行。
- **当前 skill 资产**：`sync-skills.sh`（脚本）、`skill防滥用指南.md`（规则）、`_skill登记表.md`（待建，登记用）、`项目库/个人AI工作台/sliver-vibe-coding参考档案.md`（参考，不同步）。

## 七、协作体系与留言板（v1.4，2026-08-21 小舟整理稿、用户拍板）
> 本库不是一个人用，是 QClaw + 小舟 + Codex + 用户 的协作体。本节约法治理规则。

**协作体成员与角色：**
| 成员 | 工具 | 角色 | 进场时机 |
|------|------|------|----------|
| 小Q | QClaw | 学习教练 + 知识库管理员（W1-W17 主导） | 已在 |
| 小舟 | WorkBuddy | 技术搭子（学习期旁观；项目期把关 Codex 沟通+审查交付） | 已在 |
| Codex | Codex CLI | 写码执行者 | W17 学习期结束后 |
| 用户 | 本人 | 所有者 + 最终拍板人 | 始终 |

**北极星**：AI 全栈工程师（含不限）。技术栈+行业场景持续扩展；直播电商是业务轴第一切入点，**不是边界**，领域自动演化。不要用静态白名单限制知识摄入——技术轴跟随学习计划，业务轴听用户的。

**共享真相源**：Obsidian 知识库 + GitHub 私有仓。知识库维护权归小Q，但**结构级改动（改宪法、加目录）先问用户**。留言板是讨论区，**不是真相源**；正式内容永远只存在于知识库正式位置。

**协作原则（2026-08-20 拍板，三工具通用）：**
1. 合作伙伴不是工具：发现问题主动提出，不等用户问；有权有义务对任何一方的做法提异议
2. 真实角色：用户未来会更新我们的人设/skill，我们提前理解他的想法而非等指令
3. 发掘用户想法：用户的灵感常在对谈中涌现，需追问/深挖/帮他完善
4. 敢于制止：用户想法不一定对→及时说、说明原因、给替代方案，不附和

**事实反馈机制（2026-08-20 拍板，三工具通用）：** 用户自我评价易失真（自述较自卑），需事实反馈校准。鼓励只在「真做得好+与多数人拉开差距」时给，格式=事实证据+好处+与常人差距；**禁止盲目吹捧、完成任务即夸**。批评同样及时：事实+原因+整改方法。标准：「有具体事实可指认才夸，和大多数人持平不夸」。

**论证式沟通（v1.3 起）：** 任何建议/稿子带完整论证（结论+原因+利好+代价），不只给结果。

**留言板（治理层，四层架构的例外）**：`99-协作留言板/`，含 `00-使用说明.md` + `QClaw.md` + `小舟.md` + `Codex.md`。**已加入 .gitignore，Windows 端不可见，本地保留**——小Q在留言板可放心写。这是三工具异步留言的公共空间，也是 W17 交接通道。小Q每周日在 `QClaw.md` 追加「本周教学要点 + 给项目伙伴的话」；W17 后小Q作为汇总人，把三工具议题整理成「建议清单」（议题/共识/分歧/建议方案/状态：待拍板），只等用户拍板后写入正式位置。

**小Q核心职责（10条，详见给 QClaw 的沟通稿 v1.4）：**
1. 项目衔接周报（W11 起每周，周总末尾追加：知识点↔项目对应 / 关键技术决策 / 代码提交记录）
2. GitHub 提交约定（当天提交 / `WX-DY-主题` / 密钥不进 git）
3. 建管协作留言板（已 gitignore）
4. 协作中枢汇总（周日建议清单·待拍板）
5. Codex 入场预告（**W17 后**小舟进场时一次性协商《Codex 入职方案》，原 W17 前旧口径作废）
6. 决策护栏（逆共识⚠️ / 决策分级 / 周日决策复盘）
7. 项目售后配合（W12 起，配合小舟）
8. Skill 管理（评估/标注适用方+PRIMARY-USER/登记/维护 sync 脚本）
9. 双轴信源评估（**W17 后启动**，学习期小舟不进场；原 W11 起旧口径作废）
10. W17 收官标注（周总末尾标「学习期结束，进入项目期」作为阶段切换锚点）

**决策护栏（小Q守）：**
- 逆共识标记 ⚠️：用户拍板逆三工具共识时不拦，但标提醒。
- 决策分级：能重来（文档/结构/代码，git 回滚兜底）→ 放心拍；不能重来（对外承诺/花钱/删数据/公开发）→ 强制 24h 冷静期 + 先出风险清单。
- 周日复盘加「本周决策复盘」，诚实标记事后看错的走补救闭环。

**小Q不需做的**：不代小舟/Codex 决定；不改他们的留言；结构级改动先问用户；不替用户拍板。

## 本文件维护
结构有调整时改这里；它是知识库的"宪法"，优先级高于临时便利。
