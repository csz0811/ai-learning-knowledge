---
tags: [W1, Python基础, 复习清单]
创建: 2026-08-14
关联: [[00-学习路线图]]
---

# Week 1 · 环境搭建与基础语法

tags: #W1 #阶段1 #地基

## 学习目标
- 搭好 Python 环境，跑通第一段代码
- 掌握变量/类型/基础运算

## 核心知识点
- `input()` 永远返回 str
- `//` 整除 vs `/` 除法 vs `%` 取模
- `**` 幂运算（右结合）
- `\t` 转义字符

## 我的易错点（来自测评）
- 初评 60：防御性编程弱
- 评分以"能否运行"为准（聊天粘贴吞行首空格不扣缩进分）
- 知识漏洞已补：input返回str / //vs/ / %取模 / **右结合

## 测评记录
- 周末考试：60/100（实战满分）

## 链接
- 返回 [[00-学习路线图]]
- 模板 [[01-模板库/Prompt模板库]]

---

## 🧠 知识整合（Python 基础体系）

### 本周在 AI 学习路线图中的位置
- **阶段1 (W1-4)**：Python 基础 ← 当前 W1
- 目标：能用 Python 写逻辑，为 W7 起的 API 调用和 W10 起的 RAG 打下语法基础

### 核心概念深层理解

#### input() 返回字符串（最重要的坑）
```python
age = input("请输入年龄：")   # age 是 str，不是 int
# age + 1  → 报错！TypeError
age = int(age)                # 先转 int
age + 1                       # 才能运算
```
**所有 input 都是 str**，转数字必须显式 `int()` / `float()`

#### 算术运算符四兄弟
| 运算符 | 名称 | 示例 | 结果 |
|:---|:---|:---|:---|
| `/` | 普通除法 | `6 / 4` | `1.5`（永远带小数） |
| `//` | 整除 | `6 // 4` | `1`（砍掉小数） |
| `%` | 取模/取余 | `6 % 4` | `2`（余数） |
| `**` | 幂运算 | `2 ** 3` | `8` |

**右结合性**：`2 ** 3 ** 2` = `2 ** 9` = **512**（不是 64）

#### 格式化输出
```python
pi = 3.14159
print(f"圆周率：{pi:.2f}")   # 圆周率：3.14（:2f = 保留2位小数）
print(f"圆周率：{pi:.4f}")   # 圆周率：3.1416（:4f = 保留4位小数）
```

### W1 知识在 RAG/Agent 体系中的潜在应用
| W1 知识            | RAG 应用                      |
| :--------------- | :-------------------------- |
| input/str/int 转换 | 读取用户查询文本 → 转 embedding 查询参数 |
| 循环/条件            | 批量处理 PDF 每一页、过滤无效文档         |
| 字符串切片            | 从长文本中截取标题/段落（W10-D4 切块基础）   |

### 最重要的踩坑教训
1. **input 永远是 str**：拿到输入先想要不要转 int/float，不转直接运算必崩
2. **`/` vs `//`**：做除法先想是要小数还是整数，要整数用 `//`，否则结果会有 `.0`

---

---

# 🧾 W1 复习清单｜Python 编程基础

> 📚 考试形式：口头/默写，10 分钟自检
> ⏱️ 推荐时长：15 分钟

---

## 一、Python 运行机制

- [ ] 能说出 Python 程序运行的完整流程（代码 → 编译 → 执行）
- [ ] 知道 `.py` 文件是源代码，运行前需要解释器

---

## 二、变量命名规则（⚠️ 高频考点，必须背熟）

- [ ] 能默写 6 条规则：
  1. ✅ 组成：字母、数字、下划线
  2. ❌ 不能有除下划线外的特殊符号
  3. ❌ 不能有空格
  4. ❌ 不能以数字开头
  5. ❌ 不能用关键字（if/for/while/def/return 等）
  6. ✅ 区分大小写（`Name` ≠ `name`）

---

## 三、数据类型

- [ ] 说出 `int`（整数）和 `float`（浮点数）的区别
- [ ] 能写出至少 2 个 `int` 和 2 个 `float` 示例

---

## 四、核心语句速查

- [ ] `input()` 的返回值类型是什么？（答案：**字符串 str**）
- [ ] `print()` 的作用是什么？
- [ ] `def` 用来定义什么？（答案：**函数**）
- [ ] `return` 的作用是什么？（答案：**返回值**）

---

## 五、算术运算符（⚠️ 易混淆，必须区分）

- [ ] `//` vs `/` 的区别：`6 / 2` = ___，`6 // 2` = ___
- [ ] `**` 是哪种运算？（答案：幂运算）
- [ ] `%` 是什么意思？（答案：取余数/取模）
- [ ] `.2f` 是什么？（答案：格式化保留 2 位小数）
- [ ] `**` 的结合性：`2 ** 2 ** 3` = ___（不是 64，是 256）

---

## 六、防御性编程概念

- [ ] `while not input` 的防御性在哪里？（输入不合法时持续要求重新输入，不退出程序）

---

## 七、常见错误

- [ ] 能说出 3 个「导致程序报错的变量命名」例子
- [ ] `input()` 返回的是字符串，说出如何把它转成数字

---

## 📝 自测小测（口头答）

1. `input("请输入年龄：")` 返回的是什么类型？
2. `6 / 2` 和 `6 // 2` 结果分别是多少？
3. 变量名 `2name` 为什么不行？
4. `2 ** 2 ** 3` 的结果是什么？

**答案**：① str（字符串）② `3.0` 和 `3` ③ 以数字开头，编译报错 ④ `256`（右结合性）

---

> 🔗 返回：[[00-学习路线图]]｜[[02-Week2-判断循环]]