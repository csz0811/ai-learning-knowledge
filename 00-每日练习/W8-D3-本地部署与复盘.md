# W8-D3 本地部署 + 阶段复盘（收尾 · 2026-08-16 凌晨）

> 前置：W8-D1（session_state/columns/download/uploader）、W8-D2（sidebar/视图切换/温度滑杆）。

---

## 一、本地部署跑通 ✅
- 应用已在本地运行：**http://localhost:8501** = 你的应用"部署"在自己机器上
- 这就是 W8 的「本地部署」；公网 / 服务器部署是 **W16-17** 的事

## 二、交付清单（从零跑起来）
```bash
cd ~/ai-assistant
source venv/bin/activate
pip install -r requirements.txt
streamlit run streamlit_app.py
# 浏览器开 http://localhost:8501
```
前置：`.env` 里要有 `DEEPSEEK_API_KEY`

## 三、W7 → W8 全链路复盘
| 周/天 | 学会 | 产物 |
|-------|------|------|
| W7 ① | temperature / max_tokens 调参 | day3_demo |
| W7 ② | 结构化输出（JSON mode） | content_generator |
| W7 ③ | 错误码 429 / 401 / 400 | 重试逻辑 |
| W7 ④ | Streamlit 基础 | streamlit_app v1 |
| W8-D1 | session_state / columns / download / uploader | streamlit_app v2 |
| W8-D2 | sidebar / 视图切换 / 温度滑杆 | streamlit_app v3 |
| W8-D3 | 本地部署 + 交付 | requirements.txt |

## 四、W8 结业
- 从「不会写 def」→ 做出**能用的网页版 AI 内容生成器**
- 全栈雏形：**API 调用 + 结构化输出 + 网页 UI + 本地部署**
- **W8 正式完成** 🎉

## 下一步
W9（主题看路线图）；或回看整条链路做更深复盘 / 加新内容类型。
